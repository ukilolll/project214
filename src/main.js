import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')

//ref , reactive ถ้ามีการเปลี่ยนค่าdata template จะเปลี่ยนด้วย
//mounted() do when open app
